import { BatchSplitPipe } from './batch-split.pipe';

describe('BatchSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new BatchSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
